services:
  - type: web
    name: mediscan-flask
    env: python
    buildCommand: "pip install -r requirements2.txt"
    startCommand: "python app.py"
    envVars:
      - key: FLASK_ENV
        value: production
